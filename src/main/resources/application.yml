spring:
  application:
    name: app-gestion-de-cursos
  datasource:
    url: jdbc:mysql://localhost:3306/course_management?useSSL=false&serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true